import React from "react";
import { View, Text } from "react-native";

const CurrencySelector: React.FC = () => {
  return (
    <View>
      <Text>CurrencySelector</Text>
    </View>
  );
};

export default CurrencySelector;
