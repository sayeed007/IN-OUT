import React from "react";
import { View, Text } from "react-native";

const AccountSelector: React.FC = () => {
  return (
    <View>
      <Text>AccountSelector</Text>
    </View>
  );
};

export default AccountSelector;
